const disemvowel=(str)=>str.replace('/[aeiou]/gi', '');
//use of regular expression to match a pattern of characters 
//g is for a gobal search, so it doesn't stop at the first match it founds
//i is for case-insensitive search